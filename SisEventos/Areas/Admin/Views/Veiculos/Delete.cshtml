@model SisEventos.Models.Veiculo;
@{
    ViewData["Title"] = "Delete";
}
<h2>Apagar veiculo</h2>
<p>Tem certeza que quer apagar o veiculo <strong>@Model.Nome</strong> ?</p>

<form asp-controller="Veiculos" asp-action="Delete" method="post">
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <a asp-area="Admin"
               asp-controller="Estacionamentos"
               asp-action="Index"
               class="btn btn-default">Cancelar</a>
            <button type="submit"
                    class="btn btn-danger">
                Apagar
            </button>
        </div>
    </div>
</form>
